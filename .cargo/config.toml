[target.aarch64-unknown-linux-gnu]
# 指定交叉编译的 gcc（链接器）
linker = "aarch64-linux-gnu-gcc"
# 可选：指定 ar 工具（静态库打包）
ar = "aarch64-linux-gnu-ar"
# 传递给链接器的参数（适配 Linux 规范）
rustflags = [
    "-C", "link-arg=-Wl,--as-needed",
    "-C", "link-arg=-Wl,-z,relro,-z,now",
    "-C", "link-arg=-pie"
]